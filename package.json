{
  "name": "solsense-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm start --prefix backend",
    "frontend": "npm start --prefix frontend",
    "ml": "cd ml-model && python model.py"
  }
}
